/** Meta gamificada de indicações confirmadas na área do responsável (v1). */
export const REFERRAL_GOAL_COUNT = 5;

/** Status da inscrição indicada que contam como "confirmada". */
export const REFERRAL_CONFIRMED_STATUSES = ["APPROVED", "SEMIFINALIST", "WINNER"] as const;
